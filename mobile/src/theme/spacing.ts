/**
 * Spacing scale and sizing tokens.
 * Tap targets are at least 48 (per spec — friendly for less-literate users).
 */

export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
  xxxl: 48,
};

export const radii = {
  sm: 6,
  md: 12,
  lg: 18,
  pill: 999,
};

export const fontSizes = {
  xs: 12,
  sm: 14,
  body: 16,         // minimum body per spec
  button: 18,       // minimum button label per spec
  title: 22,
  heading: 28,
  display: 34,
};

export const tapTarget = {
  min: 48,          // minimum tap target per spec (48x48dp)
};
